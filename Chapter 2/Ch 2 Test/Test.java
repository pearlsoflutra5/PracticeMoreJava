class Test {
	//don't always follow everysingle word and capatilization because sometimes they have debugging assignments where you have to catch the problems by what you know the code takes to run properly.
	public static void main(String[] args) {
		double i = 50.0;
		double k = i + 50.0;
		double j = k + 1;
		
		System.out.println ("j is " + j +" and k is " + k);
	}
}